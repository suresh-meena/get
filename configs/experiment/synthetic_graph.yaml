name: "synthetic_graph"
device: "auto"  # auto | cpu | cuda
inference_mode_train: "fixed"
inference_mode_eval: "armijo"
compile:
  enabled: true
  scope: "all"  # all | eval_only
  allow_double_backward: false
  backend: "inductor"
  dynamic: true
  fullgraph: false
  mode: null
